ON ERROR GOTO XXXX

================================
BASIC のエラーが発生すると、H.ERRO(FFB1H) が呼ばれる。
BACON は、起動時に各種フックを掛けており、そのまま BASIC へ戻ると次の実行時に
二重にフックを掛けて暴走してしまう。
これを防ぐために、エラー発生時に撤収処理を実行してから戻る。

一方で、ON ERROR GOTO XXXX にも対応させるため、このフックの飛び先で各種処理を実施する。

